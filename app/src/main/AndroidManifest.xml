<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.swigtest">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".CreateDrawFluidActivity"></activity>
        <activity android:name=".CreateFluidTouchWorldActivity" />
        <activity
            android:name=".AppInformationActivity"
            android:label="@string/title_activity_information"
            android:theme="@style/NoActionBar" />
        <activity android:name=".TrimmingPictureActivity" />
        <activity android:name=".CreateSelectionActivity" />
        <activity android:name=".FluidPlayActivity" />
        <activity android:name=".CreateFluidWorldMenuActivity" />
        <activity android:name=".SelectPictureActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>